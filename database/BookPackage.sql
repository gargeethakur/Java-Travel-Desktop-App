use TravelManagementSystem;

create table bookpackage(username varchar(20), package varchar(30), person varchar(20), id varchar(30), number varchar(30), phone varchar(15), price varchar(20));

select * from bookpackage;