create database TravelManagementSystem;

use TravelManagementSystem;

create table account(username varchar(20), name varchar(30), password varchar(20), security varchar(50), answer varchar(50));

show tables;

select * from account;